---
import LandingPage from './LandingPage.astro';
import LayoutWithTopNavBar from '../layouts/LayoutWithTopNavBar.astro';

/* TODOs

split professionell on nesling.com to private webie.at
nesling com can be hosted on github, webieat should be hosted somewhere else (to upload singular (data) files)

private:
filter lists (similar to coding projects) -> Games (can link to Blog entries), Travels?, Events, 
lexikon -> politics, history?
blog entries -> travels, IT?? on nesling.com
collections? -> LTBs (with stats?), 

some behind login?


*/
---

<LayoutWithTopNavBar>
    <LandingPage />
</LayoutWithTopNavBar>